## 2026-01-26 - Missing Empty States in Lists
**Learning:** The `IngredientsList` component lacked any empty state, showing just an empty table. This seems to be a common pattern in list components here.
**Action:** When working on lists (e.g., Suppliers, Recipes), always check for and add a friendly empty state using the `Package` icon (or relevant icon) and helpful text.
