## 2024-05-23 - Inventory Stock UX
**Learning:** Visual empty states with action buttons significantly improve the user experience compared to text-only messages. In this case, adding a "Clear Search" button to the empty state prevents users from getting stuck in a "no results" view.
**Action:** When designing list views, always include a visual empty state and, if filtered, a way to reset filters directly from that state.
