## 2024-05-22 - Search Input Patterns
**Learning:** Standard Shadcn inputs lack built-in search/clear icons, requiring manual wrapper div with relative positioning.
**Action:** When implementing search filters, always wrap `Input` in a relative div with `Search` icon (left) and `X` clear button (right) for consistent UX.
